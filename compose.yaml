services:
  rars-mcp:
    build: .
    container_name: rars-mcp
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./workspace:/workspace
    environment:
      RARS_EXECUTION_TIMEOUT_MS: "10000"
      RARS_MAX_OUTPUT_BYTES: "1048576"
      RARS_MAX_INSPECTION_BYTES: "65536"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:3000/health"]
      interval: 5s
      timeout: 3s
      retries: 10
